"""The Python Engine — course companion library.

A small, well-typed Python package that holds the reusable building blocks
taught in the course: a universal market-data adapter, feature engineering,
models, a backtester, options analytics, scanners, and risk utilities.

Modules are intentionally independent so a reader can dip into one without
needing the rest.
"""

from __future__ import annotations

__version__ = "0.1.0"

__all__ = ["__version__"]
